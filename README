This project aim is to estimate the pose of an unmanned aerial vehicle
using a monocular camera system and the help of its IMU to further
correct errors inherent to the system.

To compile project, just execute the Makefile.

Use:
username@computer:$ ./exec VideoFilePath
username@computer:$ ./exec CameraId

Example:
username@computer:$ ./exec Video.m4v
username@computer:$ ./exec 0
